{
  "mcpServers": {
    "ynab": {
      "command": "uvx",
      "args": [
        "mcp-server-for-ynab==0.5.0",
        "stdio"
      ],
      "env": {
        "YNAB_API_KEY": "${YNAB_API_KEY}",
        "YNAB_PLAN_ID": "${YNAB_PLAN_ID}"
      }
    }
  }
}
